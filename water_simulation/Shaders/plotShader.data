`
#include <engine/Math/Mat4.hpp>
`

struct PlotShaderVertex {
	Vec3 position;
	Vec3 normal = `Vec3(0.0f)`;
}

shader PlotShader {
	vertexStruct = PlotShaderVertex;
	vertInstance = {
		Mat4 transform;
	};
}